---

- hosts: localhost
  connection: local
  tasks:
    - name: show library
      command: chflags nohidden {{ home_dir }}/Library
      tags: stat

  roles:
    - brew
    - cask
    - defaults
    - go
    - highlight
    - nodejs
    - python
    - ruby
    - ssh
    - workspace
